/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      unoptimized: true, // Disable Next.js image optimization to prevent compression!
    },
  };
  
  export default nextConfig;
  